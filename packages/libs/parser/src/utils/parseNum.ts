/**
 * Parses a text and returns its numeric value.
 *
 * @param value - The text to parse.
 * @returns The value as number.
 */
export default function parseNum(value: string): number {
  return Number(value)
}
