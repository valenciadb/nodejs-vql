/**
 * A parameter bind: &name.
 */
export default class ParamBind {
  /**
   * Creates a parameter bind with the given arguments.
   *
   * @param name - The parameter name.
   */
  constructor(readonly name: string) {}
}
