/**
 * A key field.
 */
export default class KeyField {
  /**
   * Creates a key field using the given arguments.
   *
   * @param name - The key field name.
   */
  constructor(readonly name: string) {}
}
